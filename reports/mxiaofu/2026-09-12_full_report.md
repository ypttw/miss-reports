# M小福 2026-09-12 全專案白話補充總報告

補報製作日：2026-09-13（Asia/Taipei）  
報告日期：2026-09-12（星期六）  
查核性質：以既有 9/12 唯讀報告、注單附件、9/11 常勝軍個案與現行規則文件做白話補報；本檔不是重新執行一輪正式系統查核。

## 先看結論

9/12 已有一份包含部分全專案章節的初報，也另有白話注單附件；兩者沒有整合成一份讓讀者先看懂「發生什麼、影響什麼、是否修復、下一步」的全專案讀者版。這份文件把兩類既有來源整合，也把舊數字和現在能證明的範圍分開。

先用白話看結論：

- 發現什麼：注單對照不是完全沒有問題；舊附件中有 5 個需要續追的事件，其中日期被抓成號碼、金額被抓成號碼等語意差異已能直接看出，另有金額差異與分頁缺列仍待定位。
- 影響什麼：目前的來源集合不完整，不能宣稱 9/12 全天零漏收、零重複；舊附件所列的來源分頁比對，也不能代替下注程式當時讀取內容或網站受理。
- 常勝軍怎麼了：9/11 的圖片確實在 MISS ORDER 收到，但沒有證據接到 MISS 的 B12；缺少下一段紀錄是交接缺口，不是「沒有收到」的根因證明，原因仍待查。
- 有沒有修復：沒有。本輪只整理既有唯讀查核紀錄，沒有改正式程式、重送、補寫、下注、重啟或部署。
- 下一步：補齊來源全集，逐筆串接收圖、OCR、寫入、讀回、產圖與送達；同時把 5 個注單差異、ORDER 傳送失敗紀錄，以及收單守護／主程式異常接回同一筆事件。

本補報的狀態是 `READ_ONLY_REPORT`。本次沒有 replay、resend、下注、GS/DB/F1/outbox 寫入、restart、deploy 或程式修改；`DEPLOYED` 未發生，`RUNTIME_LOADED=UNKNOWN`，`LIVE_ACCEPTANCE_PASS` 與 `PRODUCTION_STABLE` 都沒有宣稱。

## 1. 先釐清數字與證據界線

### 1.1 舊報告的 12、54、109 分別代表什麼

早期的 12 筆是手動挑出的例子。後來的 54 個事件／109 列，是沿用 MISS 考卷窗口 **2026-09-11 20:31 至 2026-09-12 20:30（台灣時間）**，從資料庫中有保存解析內容的事件所做的逐筆核對。它沒有涵蓋：雲端未送到本機的事件、沒有解析列的事件、只有原始來源而未保存解析的事件，以及所有 20:30 後續作業。因此本報告不把 54/109 寫成全日總數，也不把 12 寫成正式分母。

### 1.2 YPM／小新與 PY 的正確說法

舊附件把 YPM／小新標成 `combined_bet.py` 的直接下注來源「不適用」，意思只到那個程式入口沒有直接讀它們；不能由此推論整個專案的下注程式都沒有讀 YPM／小新，也不能把整筆注單當成自動不適用。YPM、記錄與小新仍要按實際路由、消費程式與當時保存的輸入逐層核對。這次補報沿用舊附件的局部來源分頁比對，但沒有證明下注程式當時讀到的輸入或執行快照；現在讀到的 GS／分頁內容不能回推過去下注程式已讀。

### 1.3 沒有 `editor_revisions` 代表什麼

舊附件多筆寫「沒有查到後續 `editor_revisions`」。這只代表目前來源沒有另存的修訂紀錄；不代表使用者從未修改，也不代表當時保存解析就是最後採用內容。最後採用版本與網站 receipt 是執行層的另一組證據，缺少時只對那一層標 `UNKNOWN`，不阻止清楚的原文與保存解析差異成立。

### 1.4 缺 `event_id` 代表什麼

缺 durable `event_id`、圖片檔或 terminal log，只能說目前證據無法把事件接到下一層；不能反推「沒有收到」或直接指定 OCR、GS、Telegram polling 為根因。每一個結論都要分開寫到哪一層有證據、從哪一層開始沒有證據。

## 2. MISS 收單、漏單／重複、解析與 PY

### 2.1 收單完整性目前不能判全天通過

舊注單附件只從「有解析內容的 event」開始，並非 LINE、Telegram、Macro、SQLite、YPM、記錄、小新與 PY 的完整雙向全集。因而目前不能宣稱 9/12 全天零漏收、零重複、零延遲，也不能用 49/54 的分頁結果代替來源覆蓋率。

可確認的做法是使用同一個 `event_id` 串接原文／圖片、保存解析、目標分頁與適用的下注程式當時讀取內容。可確認的限制是：分頁一致只證明那一層一致；網站實際接受要另有 exact receipt。這份附件沒有完成全日網站受理與執行回執的建鏈。

另有 9/11 舊漏寫紀錄：05:31 三筆中只有一筆在 07:50 自動走完，07:30 一筆未自動寫入，後續三筆由使用者以 exact source event 手動補入；當時入口曾有 HTTP 502／Timeout。這是前一天的歷史個案，不是 9/12 全日統計，也沒有在本補報中重新查正式系統。

### 2.2 解析／分頁／PY 的已知結果

| 層次 | 既有證據能說什麼 | 仍不能說什麼 |
|---|---|---|
| 保存解析到目標分頁 | 54 個事件中 49 筆相符、3 筆內容不同、2 筆缺同一列 | 不能說來源全集都已收到，也不能說網站已受理 |
| 舊附件中的 539 欄位比對 | 9/9 個適用事件在附件列出的來源分頁欄位相符 | 本補報沒有驗證下注程式當時讀取內容／執行快照；不能回推當時已讀 |
| 舊附件中的天天樂欄位比對 | 3/3 個適用事件在附件列出的來源分頁欄位相符 | 本補報沒有驗證下注程式當時讀取內容／執行快照；不能代替執行與結算 |
| 舊附件中的散單欄位比對 | 2/5 找到同一 `event_id` 且附件欄位相符，3/5 缺同一 ID | 缺列原因、下注程式當時讀取內容與實際受理仍 UNKNOWN |
| YPM／小新 | 各自分頁有做部分核對 | 沒有重新證明所有實際消費路徑與當時讀取內容 |

### 2.3 5 個要續追的事件

| `event_id` | 白話問題 | 目前可證明的影響 | 狀態 |
|---|---|---|---|
| `631393184504873511` | 小新第一行的 `9/12` 被抓成 `09,12`，造成 35 列解析對 34 列分頁，後面錯位 | 原文語意已足以列為日期誤抓的解析錯例；後續受理另屬執行層 | `解析錯例` |
| `631464422124028109` | YPM 前兩列二星解析 70、80；分頁為 20、50，三星相同 | 可能是圖片解析或寫入端差異，第一個錯誤層未定位 | `UNKNOWN` |
| `631465498483359907` | 「10、33、車、各 5 元」被解析成號碼 10、33、05，漏了全車 5 | 原文語意已足以列為金額誤抓的解析錯例；後續受理另屬執行層 | `解析錯例` |
| `631354634019537033` | 查 8/24-8/29 總帳的聊天被抓成 08、24、29 注單候選 | 沒有同一記錄分頁或散單 PY；不能算成漏單 | `非注單候選` |
| `631458503575732582` | 解析到 06、11、25、31 與星級金額，但小新分頁找不到同一 ID | 可能是寫入、清理或路由缺口，現有資料不能區分 | `UNKNOWN` |

這 5 筆是後續跨日產出的注單附件列出的查核結果，這裡的錯例是「原文」與「目前可取得的保存解析」不同；缺少 first-parse snapshot 時，不能冒稱這就是第一次解析，也不能推論沒有人工調整。日期被抓成號碼、金額被抓成號碼的兩筆，原文語意已足以列為解析錯例；這個判定不需要先找到最後採用內容或網站回條，也不表示已修改 parser。YPM 金額差異、查帳訊息候選與小新缺列則仍要定位第一個出錯層；任何實作仍須另外取得使用者同意。網站 receipt 是執行驗收層，只影響那一層是否能判定，不會推翻清楚的原文與保存解析差異。

## 3. 重試、待處理／傳送失敗、收單守護與開獎報帳

### 3.1 重試與待處理狀態

- MISS 5005 的 9/12 快照為 `/healthz=200`、writer `healthy`、`writerBlocked=false`、`pendingCount=0`。這只表示查核當下沒有本機 pending；不證明前一段時間沒有漏單，也不證明某張圖片已完成整條流程。
- B12 舊快照有 2 個 `READY` batch，其中一張小新不中圖片仍 `PENDING`、receipts 為 0。這是明確的「準備完成」與「送達完成」分離案例。
- MISS ORDER `tg_inbox` 共有 1101 筆且都 `processed`，沒有 pending/dead inbox；`tg_outbox` 有 21 筆 `dead/copy_message`，錯誤為 `BadRequest: The message can't be copied`。既有來源沒有證明這 21 筆是否影響 9/12 哪一筆業務結果，也沒有在本輪自動重試。
- 重試次數本身不能代替終點。要判斷某筆是否完成，仍需同一事件的 payload、attempt、回應、目的地與 final receipt；目前不少事件缺其中一段。

### 3.2 收單守護與主程式線索

- `collector_guardian.log` 在 01:05、02:12、17:24 出現 health failure/recovery；這表示收單守護曾發現異常並恢復，不表示每一筆事件都被補回。
- `tg_guardian.log` 有 daemon not running/start 紀錄；這表示收單程序曾有啟停異常，不表示某張 Telegram 圖片就是因此遺失。
- 12:37 有 W run unavailable；目前沒有足夠 evidence 把它和某一筆下注、重複或正式漏單配對。
- 另有歷史 Chang 後段 Windows access violation，但沒有本次訊息的事件身份。它是待查的歷史線索，不能寫成 9/12 的 crash 根因；本輪也沒有啟動或重啟 W。

## 4. 開獎報帳：從收圖到送達逐層看

| 階段 | 已知證據 | 現在可以下的結論 | 尚未完成的部分 |
|---|---|---|---|
| TTL 開獎來源 | `b12_fastpath_reports` 有今日天天樂 `READY`；source event `01M29MQF64KVY4P09YGZJZK544`，號碼 `3,9,16,25,30`，有 fresh read/readback | TTL 的準備與讀回在既有快照中可見 | 不等於所有彩種都完成，也不等於通知送達 |
| 539 路由 | 9/12 `b12_draw_progress.log` 可讀 87 行，其中 83 行 `route=ttl_draw`、80 行 terminal `NO_MAPPING`；沒有 `route=539`／`route=chang` | 這是來源與路由異常線索 | 來源、事件身份與第一個錯誤層未重新定位 |
| 常勝軍 ORDER 收圖 | 9/11 `message_id=99`、chat `-1003804335513`、sender `8817839914` 的完整 photo payload 在 ORDER `tg_inbox`，標 `processed` | 至少證明 ORDER 收到並返回；不是證明 B12 完成 | ORDER 到 MISS 的 handoff 沒有 exact receipt |
| MISS durable event／圖片存檔 | 查核快照沒有該群事件、該 message 的 MISS `event_id` 或圖片 archive | 沒有證明事件進入 MISS B12 | 缺口不能直接當作「未收到」根因 |
| OCR | 沒有該圖的 OCR 結果或完整 OCR input/版本 | `UNKNOWN` | 不能把問題先歸給 OCR |
| write／readback | 沒有該圖對應的 B12 write/readback receipt | `UNKNOWN` | 不能說 GS 或 writer 是第一根因 |
| render／send | 沒有該圖的產圖與 Telegram send receipt | `UNKNOWN` | 不能把成功通知或別張圖代替本圖 |

9/11 個案另確認：ORDER 使用的接收 Bot 是 `@baobaofu_bot`，MISS 既有產圖流程使用共用 `@missfufu_bot`；不能直接更換共用 token，因為會影響其他訊息功能。這是待核准的整合方向，不是本輪已修復結果。

另有先前測試事件 `tg_m1004415191836_6` 曾走到 MISS／route，但被 `REPORT_DATE_REQUIRED` 擋住。這只是先前測試證據；不代表 9/12 現在仍同樣，也不能當成當日完整收圖、OCR、寫入、讀回、產圖、送達成功。

## 5. MISS ORDER 與兩專案交接

### 5.1 MISS ORDER 當日可見結果

- `registration_events` 有 4 筆 `accepted`，customer id 為 182、200、185、206。
- `2026-09-12:15:00` wave 為 `released`；snapshot publication 為 `success`、attempt 1。
- 以上是 ORDER 本地資料層的結果；目前不能用它們替代 GS readback、F1 狀態、MISS 實際執行或網站回執。
- frozen wave 與目前 daily state／registration event 的 identity 對應不完整，不能用現在 customer state 覆蓋當時 frozen manifest。

### 5.2 交接要用整條鏈看

交接不能只看一個成功標記。ORDER 主流程要逐段核對開獎批次、工作表／橋接讀回、工作旗標、MISS 執行到網站受理；常勝軍圖片要逐段核對收圖、ORDER 收件、交給 MISS、寫入與讀回、產圖到 Telegram 通知。原始紀錄中的 `processed`、`HANDED_OFF` 或單一 HTTP 200 都只代表其中一段；缺任一事件身份或最後回條就保留 `UNKNOWN`。（完整串接欄位仍見括號中的 `wave -> GS/bridge readback -> F1 -> MISS 執行 -> 網站受理` 與 `exact photo -> ORDER inbox -> handoff -> MISS B12 -> write/readback -> render -> Telegram send`。）

對 `CHANG_TG_PHOTO_20260911_2034`，已知的是 ORDER 收到 message 99 且已處理；MISS 端沒有可配對的事件或進度紀錄。現在能指出的是「ORDER 收到後到 MISS B12 之間的交接沒有被證明」，但還不能把原因指定為收訊、轉交、Bot 身分或其他入口分支。

## 6. 昨日未結案與 9/12 實際處置

### 6.1 從 9/11 延續到 9/12 的未結案

1. 常勝軍 message 99 從 ORDER 交給 MISS 到 OCR、寫入與讀回、產圖、通知還沒有完整紀錄，原因 `UNKNOWN`。
2. 開獎報帳的 `NO_MAPPING` 與小新圖片 `PENDING` 尚未逐事件對上，不能只因 `READY` 就結案。
3. MISS ORDER 有 21 筆傳送失敗紀錄，還沒有完成完整內容與業務影響分類。
4. 收單守護／主程式的異常還沒有和同一事件、受影響時間及最後結果完整對上。
5. 注單附件缺少完整來源集合、最後採用版本、部分下注程式當時讀取內容與網站回條；54/109 仍是窗口內非空解析子集合。

### 6.2 9/12 歷史查核快照實際做了什麼

- 9/12 21:30 的舊初報以既有事件編號、SQLite、分頁、開獎進度、MISS ORDER SQLite／個案文件做唯讀整理。
- 9/12 22:40 的歷史快照對常勝軍身份、RouteMap、MISS 事件／圖片 archive／B12 progress／5005 health 做後續只讀查核；沒有重送或補寫。
- 9/12 21:30／22:40 的初報快照沒有完成那 5 個注單差異的結果；後續跨日產出的白話注單附件才另外列出 5 個解析／分頁差異。這兩批證據要分開，不能把後來附件的結果倒填成當晚已完成。
- 當時把注單解析與後續報帳／結算／通知分開；後續附件也保留 5 個差異與常勝軍交接缺口，沒有把 UNKNOWN 填成根因。
- 當時沒有修改 parser、OCR、路由、Bot token、設定、DB、GS、F1、queue 或 outbox；沒有部署、重啟或下注。

資料日期、查核日期與補報日期要分開：注單窗口是 9/11 20:31 至 9/12 20:30；ORDER／開獎報帳與目前運行程序的上述快照是在 9/12 21:30／22:40 讀取；本補報與 PDF 渲染是在 9/13 完成。本補報製作只是重新編排上述已讀來源，沒有新增實戰證據，也沒有把舊報告的歷史版本當成目前運行版本已載入的版本。

### 6.3 版本、設定維護與工作日誌

- 本補報回合沒有重新查 `D:\MISS`、`D:\MISS_ORDER` 的目前版本、未提交檔案、程式指紋、目前運行版本或目前有效設定；初報第 1 章的 HEAD 只作 9/12 歷史快照，不能當成 9/13 當前版本。
- 9/13 主設定已由主線完成同步，並由獨立情境覆核與 skill 格式檢查；排程入口已讀回為週一至週六 21:30 的 ACTIVE。這是報告／日誌／維護流程設定處置，不是 parser、OCR、B12 或下注邏輯修復，也沒有替代實戰驗收。
- 9/13 工作日誌已追加設定同步、排程讀回與本補報續接資訊，可讀於 [`reports/mxiaofu/journal/2026-09-13.md`](journal/2026-09-13.md)。該日誌記錄的是設定維護與文件續查，不是 9/12 全日正式系統重查。

## 7. 下一步與交付限制

下一輪若要把 UNKNOWN 往下縮小，應沿同一筆事件補證據，不能用文字、時間、最新一列、健康 200 或別張圖猜配：

1. 對 9/12 先建立來源全集與查核截止時間，將「來源事件數、疑似注單、解析列、分頁列、下注程式當時讀取內容／拆單、未查缺口」分開計數。
2. 對 5 個差異保留原文／原圖與保存解析；日期誤抓、金額誤抓等語意明確者可直接整理為解析錯例，YPM 金額差異、缺列與非注單候選則繼續定位第一個出錯層。使用者實際採用內容、適用下注程式當時讀取內容與網站 receipt 另列為執行／歷史讀取證據，不能互相代替。
3. 對 B12 按同一筆 `event_id` 串起收圖、OCR、寫入回條、讀回、產圖檔與送達回條；不以別張圖片、健康 200 或 READY 代替。
4. 對 MISS ORDER 21 筆傳送失敗紀錄（dead outbox）查完整內容、目的地、最後錯誤與業務對應；查不到就不要自動重試或猜配。
5. 對收單守護／主程式事件補子程序／主程序、時間窗、第一個可驗證錯誤與受影響事件；歷史 crash 沒有身份就維持線索，不升級成根因。

任何 parser、OCR、路由、重試、去重、成功判定、Bot 分工或設定修改，都要先提出修改前／修改後、影響範圍、不變項與驗證案例，取得使用者同意後才可做 candidate；本報告本身不含候選修復。

## 來源與限制

本補報使用的主要來源與其角色如下：

- `MISS_NIGHT_OPS_AGENT_PLAN_20260912.md`：現行 9/13 規則，尤其第 4、11、15、16 節；規定全專案報告、窗口界線、逐層證據及 UNKNOWN 處理。
- `reports/mxiaofu/2026-09-12.md`：9/12 夜間唯讀快照；包含目前運行程序、B12、收單守護、MISS ORDER 與常勝軍後續查核結果。它是歷史快照，不是本補報製作時重新驗證的當前狀態。
- `reports/mxiaofu/2026-09-12_plain_language.md`：9/12 舊注單附件；包含 54/109 子集合、5 個差異與逐筆比對。它不是全專案報告，也不是全日完整母體。
- `MXIAOFU_CASE_20260911_CHANG_TG99.md`：9/11 常勝軍 message 99 的事件身份、ORDER 收件、Bot／路由與待核准方向；沒有部署或 live 修復證據。

必要限制：目前沒有在本補報回合重新讀取 D:\MISS、D:\MISS_ORDER 的正式資料庫、工作表、Telegram、網站或目前運行程序（production DB／GS／resident runtime）；因此本檔不能宣稱 9/13 當下狀態，也不能把文件中的本地成功紀錄升級成正式業務成功。
